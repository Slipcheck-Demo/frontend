# betway-booking-frontend

Next.js UI for the Betway Nigeria booking-code product (Decode / Create / Convert).
Talks to [`betway-booking-backend`](../betway-booking-backend) — never calls Betway directly.

Not started yet. See the shared plan and API contract in the `stellar-test-task` repo
(`docs/plan.md`, `docs/betway-api.md`) until those are copied/synced here.
